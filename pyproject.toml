[project]
name = "samsa"
version = "0.1.0"
description = "Module for symmetry analysis of discrete data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyterlab==3.*",
    "numpy==2.*",
    "scipy==1.17.*",
    "toml==0.10.*",
]
